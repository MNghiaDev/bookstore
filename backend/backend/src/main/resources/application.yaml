server:
  port : 8080
  servlet:
    context-path: /api/bookstore
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/bookstore"
    username: root
    password: root
    jpa:
      show-sql: true
      hibernate:
        ddl-auto: none